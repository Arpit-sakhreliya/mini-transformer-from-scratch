# mini-transformer-from-scratch
A from-scratch NumPy implementation of a tiny decoder-only Transformer (mini-LLM). It includes embeddings, self-attention, MLP blocks, layer norm, backpropagation, and next-token prediction trained on a toy dataset. Designed to clearly show how GPT-style models learn step by step without deep learning frameworks.
